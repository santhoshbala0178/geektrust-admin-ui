import React from "react";
import { SearchBarInput } from "./SearchBar.style";

const SearchBar = () => (
  <SearchBarInput placeholder="Search by name, email or role" />
);

export default SearchBar;
